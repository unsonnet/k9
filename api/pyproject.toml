[project]
name = "k9-api"
version = "0.1.0"
description = "K9 API Backend"
authors = [{ name = "K9 Team" }]
readme = "README.md"
requires-python = ">=3.11,<3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = ["-v", "--tb=short", "--strict-markers"]

[tool.black]
line-length = 88
target-version = ['py311']

[tool.isort]
profile = "black"
multi_line_output = 3

[tool.mypy]
python_version = "3.11"
strict = true

[tool.ruff]
line-length = 88
target-version = "py311"
select = ["E", "F", "I", "N", "W"]

[dependency-groups]
dev = [
    "black>=25.9.0",
    "boto3-stubs[cognito-idp,dynamodb,essential,opensearchserverless,s3]>=1.40.55",
    "isort>=7.0.0",
    "lark>=1.3.0",
    "mypy>=1.18.2",
    "pytest>=8.4.2",
    "pytest-cov>=7.0.0",
    "pytest-mock>=3.15.1",
    "regex>=2025.10.22",
    "ruff>=0.14.1",
    "types-requests>=2.32.4.20250913",
]

core = [
    "boto3>=1.40.55",
    "pydantic>=2.12.3",
    "python-jose[cryptography]>=3.5.0",
    "requests-toolbelt>=1.0.0",
]

data = [
    "numpy>=2.3.4",
    "pillow>=12.0.0",
]

ml = [
    "opensearch-py>=3.0.0",
]
