[project]
name = "infra"
version = "0.1.0"
description = "CDK infrastructure for K9 API"
requires-python = ">=3.12"
dependencies = [
    "aws-cdk-lib>=2.250.0",
    "constructs>=10.6.0",
    "pydantic>=2.12.5",
]

[tool.uv]
package = false
